import { ChainConfig } from "./types.js"; export const CHAINS: Record<string, ChainConfig> = { robinhood: { name: "Robinhood Chain", chainId: 4663, rpcUrl: "https://robinhood-mainnet.g.alchemy.com/v2/alch_FtrEfyyJYzEBZ0SQ3ctbJ", explorerUrl: "https://robinhoodchain.blockscout.com", nativeCurrency: "ROB", decimals: 18 }, base: { name: "Base", chainId: 8453, rpcUrl: "https://mainnet.base.org", explorerUrl: "https://basescan.org", nativeCurrency: "ETH", decimals: 18 }, ethereum: { name: "Ethereum", chainId: 1, rpcUrl: "https://eth.llamarpc.com", explorerUrl: "https://etherscan.io", nativeCurrency: "ETH", decimals: 18 } }; export function getChain(name: string): ChainConfig { const chain = CHAINS[name.toLowerCase()]; if (!chain) throw new Error(`Chain ${name} not supported`); return chain; } export function listChains(): string[] { return Object.keys(CHAINS); }
